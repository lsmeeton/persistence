#!/usr/bin/env python

from distutils.core import setup

setup(name='persistence', 
      version='0.1', 
      description="Python implementation for calculating, plotting and storing persistance diagrams",
      url='https://bitbucket.org/lsmeeton/persistence',
      packages=["persistence", "persistence.test",],
      author_email='lcs137@bham.ac.uk',
        )